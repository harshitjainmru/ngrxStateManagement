import { TrimDirectiveDirective } from './trim-directive.directive';

describe('TrimDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
