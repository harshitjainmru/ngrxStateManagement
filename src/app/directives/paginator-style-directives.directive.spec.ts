import { PaginatorStyleDirectivesDirective } from './paginator-style-directives.directive';

describe('PaginatorStyleDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginatorStyleDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
